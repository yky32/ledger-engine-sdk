# Quickstart (UAF and any client)

→ **[QUICKSTART.md](./QUICKSTART.md)**

`forUafinance` is **deprecated**. Use:

```java
LedgerClient.forLoyalty(baseUrl);
// or pin your integration id on onboard metadata:
LedgerClient.forIntegration(baseUrl, "uafinance");
```
